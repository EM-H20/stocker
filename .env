# Flutter App 환경 변수 설정
# API 통신을 위한 백엔드 서버 URL (백엔드 PORT=8081에 맞춤)
API_BASE_URL=http://localhost:8080

# 개발/운영 환경 구분
ENVIRONMENT=development

# 디버그 모드 (API 로깅 등)
DEBUG_MODE=true

# 타임아웃 설정 (초 단위) - MySQL 연결 고려
CONNECT_TIMEOUT=15
RECEIVE_TIMEOUT=15

# JWT 관련 설정 (실제 백엔드 엔드포인트에 맞게 수정)
JWT_LOGIN_ENDPOINT=/api/user/login
JWT_SIGNUP_ENDPOINT=/api/user/signup
JWT_LOGOUT_ENDPOINT=/api/user/logout

# 백엔드는 미들웨어에서 자동 토큰 갱신 처리하므로 별도 refresh 엔드포인트 없음
# JWT_REFRESH_ENDPOINT는 사용하지 않음